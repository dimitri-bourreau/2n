import React, { ReactElement } from 'react';
import { RouteComponentProps } from '@reach/router';

const Settings: RouteComponentProps = (): ReactElement => {
  return (
    <>
      <h1>Settings En cours...</h1>
    </>
  );
};

export default Settings;
