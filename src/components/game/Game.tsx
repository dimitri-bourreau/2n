import React, { FunctionComponent, ReactElement } from 'react';
import { GameProps } from '../../interfaces/game';

const Game: FunctionComponent<GameProps> = (): ReactElement => {
  return (
    <>
      <h1 className="mt-5 mx-auto text-center">OK</h1>
    </>
  );
};

export default Game;
