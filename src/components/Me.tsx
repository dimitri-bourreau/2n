import React, { ReactElement } from 'react';
import { RouteComponentProps } from '@reach/router';

const Me: RouteComponentProps = (): ReactElement => {
  return (
    <>
      <h1>Me En cours...</h1>
    </>
  );
};

export default Me;
